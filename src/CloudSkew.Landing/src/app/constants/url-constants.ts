import { environment } from 'src/environments/environment';

// @todo: All strings must be moved into a .resx resource file.
export class UrlConstants {
    // Please keep this list alphabetically sorted.
    public static readonly landingPageFeature1Url = environment.landingPageFeature1Url;
    public static readonly landingPageHeroUrl = environment.landingPageHeroUrl;
}
